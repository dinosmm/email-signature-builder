# Logo asset

Binary image files are intentionally not committed. To use a separate school logo file, place the school's JPG or PNG in this directory and update `../defaults.js` so `schoolLogoPath` points to that filename.
