# Logo asset

Place the school's actual JPG or PNG logo in this directory and update `../defaults.js` if the filename is not `school-logo.png`.

The app references `assets/school-logo.png` by default. The logo file is intentionally not committed here because this environment cannot create PRs that include binary files.
